version: '3.8'

services:
  md-editor:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: md-editor
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
