version: '3.8'

services:
  markdown-viewer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: markdown-viewer
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
